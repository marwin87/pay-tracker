---
bootstrapped_at: 2026-06-11T13:25:00Z
starter_id: next
starter_name: "Next.js"
project_name: pay-tracker
language_family: multi
package_manager: npm
cwd_strategy: subdir-then-move
bootstrapper_confidence: verified
phase_3_status: ok
audit_command: "null"
---

## Hand-off

```yaml
starter_id: next
package_manager: npm
project_name: pay-tracker
hints:
  language_family: multi
  team_size: solo
  deployment_target: self-host
  ci_provider: github-actions
  ci_default_flow: auto-deploy-on-merge
  bootstrapper_confidence: verified
  path_taken: custom
  quality_override: false
  self_check_answers:
    typed: true
    from_official_starter: true
    conventions: true
    docs_current: false
    can_judge_agent: true
  has_auth: true
  has_payments: false
  has_realtime: false
  has_ai: false
  has_background_jobs: true
```

Pay Tracker is a solo, 7-week after-hours project with auth, background tasks
(auto-generated payment instances + email reminders), Excel/JSON export, and a
hard requirement for dual-mode deployment: self-hosted Docker Compose as the
primary target and an optional cloud mode via environment-variable switching.
The stack is intentionally polyglot: Next.js (TypeScript, App Router) is the
frontend and primary scaffolding layer — it passes all four agent-friendly gates
and ships from an official CLI (create-next-app); FastAPI (Python, Pydantic,
uv) handles the backend API, export via OpenPyXL/Pandas, auth in local mode,
and email reminders — functionality that genuinely benefits from Python's data
library ecosystem. Both starters pass all four quality gates. Bootstrapper will
scaffold the Next.js shell; the FastAPI backend is a second service scaffolded
manually alongside it and wired together by Docker Compose. No Cloudflare or
Vercel lock-in; self-host is a first-class deployment target in the Next.js
card. CI runs on GitHub Actions with auto-deploy-on-merge.

## Pre-scaffold verification

| Signal      | Value                                       | Severity | Notes                             |
| ----------- | ------------------------------------------- | -------- | --------------------------------- |
| npm package | create-next-app v16.2.9 published 2026-06-10 | fresh    | resolved from cmd_template        |
| GitHub repo | not run                                     | n/a      | docs_url is nextjs.org/docs (not GitHub) |

## Scaffold log

**Resolved invocation**: `npx create-next-app@latest pay-tracker-scaffold --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`

Note: `.bootstrap-scaffold` prefix rejected by create-next-app (npm naming restriction — names cannot start with a period). Used `pay-tracker-scaffold` as the temp directory name instead.

**Strategy**: subdir-then-move
**Exit code**: 0
**Files moved**: 15 (source files + directories)
**Conflicts (.scaffold siblings)**: `CLAUDE.md` → `CLAUDE.md.scaffold` (starter contents: `@AGENTS.md`)
**.gitignore handling**: moved silently (absent in cwd before scaffold)
**AGENTS.md**: moved silently (absent in cwd; auto-generated by create-next-app with `--agents-md` default)
**.bootstrap-scaffold cleanup**: deleted (renamed to `pay-tracker-scaffold` for this run; cleaned up after move)
**Skipped**: `.next/` build artifact (regenerated on first `npm run dev` / `npm run build`)

## Post-scaffold audit

**Tool**: skipped — no built-in audit tool for `multi` language family

**Recommended external tools**:
- JS layer: `npm audit` (run from project root; 2 moderate vulnerabilities were noted in install output — run `npm audit` to inspect)
- Python layer: `pip-audit` (install separately; run after setting up the FastAPI virtualenv)

## Hints recorded but not acted on

| Hint                    | Value                                    |
| ----------------------- | ---------------------------------------- |
| bootstrapper_confidence | verified                                 |
| quality_override        | false                                    |
| path_taken              | custom                                   |
| self_check_answers      | typed:true, from_official_starter:true, conventions:true, docs_current:false, can_judge_agent:true |
| team_size               | solo                                     |
| deployment_target       | self-host                                |
| ci_provider             | github-actions                           |
| ci_default_flow         | auto-deploy-on-merge                     |
| has_auth                | true                                     |
| has_payments            | false                                    |
| has_realtime            | false                                    |
| has_ai                  | false                                    |
| has_background_jobs     | true                                     |

None of the above hints drove scaffolding decisions in v1. A future M1L4 skill
will use deployment_target, ci_provider, ci_default_flow, and the has_* flags
to generate CLAUDE.md / AGENTS.md and CI workflow files.

## Next steps

Next: a future skill will set up agent context (CLAUDE.md, AGENTS.md). For now,
your project is scaffolded and verified — happy hacking.

Useful manual steps in the meantime:
- `git init` is already done (existing `.git/` in place).
- Review `CLAUDE.md.scaffold` — the starter's content is just `@AGENTS.md`; you
  may want to incorporate the AGENTS.md reference into your existing CLAUDE.md.
- Review `AGENTS.md` — auto-generated by create-next-app; inspect and extend it
  for the pay-tracker project specifics.
- Run `npm audit` to inspect the 2 moderate JS vulnerabilities from install.
- Scaffold the FastAPI backend manually: create a `backend/` directory, initialise
  a Python project with `uv init`, add FastAPI, Pydantic, OpenPyXL, and Pandas,
  then wire both services together with `docker-compose.yml`.
- Add a `.env.example` file documenting the LOCAL/CLOUD mode env-var switch.
