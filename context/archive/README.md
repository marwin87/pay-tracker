# Archive

Completed changes. `history.md` holds one condensed entry per change (decisions, pitfalls, files); full plans/research/reviews of compressed changes live in git history.

`/10x-archive` still moves new changes here as `<date>-<change-id>/` (read-only by convention; skills refuse to write here). Fold them into `history.md` periodically and delete the folders.
